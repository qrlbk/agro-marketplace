[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --cov=app --cov-report=term-missing --cov-report=html --cov-fail-under=0
markers =
    unit: tests that do not require DB or Redis
    integration: tests that require PostgreSQL and Redis (docker compose up)
